define([],
    function () {
        // module:
        //		sijit/ServiceManager/Plugin/PluginInterface

        return {

            // dependencies: array
            //    An array of plugin names that are required by this plugin
            dependencies: [],

            serviceManagerMixin: {
                // An object to be mixed into the ServiceManager
            },
            refExtend: {
                // An class definition to be extend the Ref class
            }
        };
    }
);

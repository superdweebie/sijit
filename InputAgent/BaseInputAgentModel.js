define ([
        'dojo/_base/declare'
    ],
    function (
        declare
    ){
        var model = declare (
            'Sds/InputAgent/BaseInputAgentModel',
            null,
            {}
        );

        model.metadata = {
            fields: []
        };

        return model;
    }
);


